# Core
flask>=3.0
sqlalchemy>=2.0
python-dotenv>=1.0
requests>=2.31

# Optional LLM providers (install only what you need)
anthropic>=0.39.0
google-genai>=1.0.0

# Optional search providers (install only what you need)
tavily-python>=0.5.0
exa-py>=1.0.0

# Testing
pytest>=8.0

# Production WSGI server (for Docker / deployment)
gunicorn>=22.0
